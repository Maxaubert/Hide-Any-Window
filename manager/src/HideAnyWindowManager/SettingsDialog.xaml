<ContentDialog
    x:Class="HideAnyWindowManager.SettingsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Settings"
    CloseButtonText="Close"
    DefaultButton="Close">
    <Grid Width="400" Padding="0,8,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <StackPanel>
            <TextBlock Text="Start service at logon" FontSize="14"/>
            <TextBlock Text="Automatically launch the AHK service when you sign in to Windows."
                       FontSize="11" Foreground="Gray" TextWrapping="Wrap" Margin="0,2,0,0"/>
        </StackPanel>
        <ToggleSwitch Grid.Column="1" x:Name="AutostartSwitch"
                      OnContent="" OffContent="" MinWidth="0"
                      Toggled="AutostartSwitch_Toggled"/>
    </Grid>
</ContentDialog>
